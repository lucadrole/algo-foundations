# Glossary

**Correctness** (in an algorithm): you never get a wrong answer, the algorithm does not loop forever.

**Recursive Algorithm**: a procedure that invokes itself as a subroutine with a smaller input, until a base case is reached.